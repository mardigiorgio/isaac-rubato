# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.eggs/

# Venvs / envs
.venv/
env_*/
venv/

# Editor / Kit / OS cruft
imgui.ini
.DS_Store
*.swp

# Build / artifact scratch (data lives outside the repo, under the data root)
build/
dist/
*.log


# W&B run cache (anywhere in the tree) + secrets that must never be committed
wandb/
.netrc
*.key

# RL training logs (checkpoints/tfevents/videos) — tracked in W&B, not git
logs/
